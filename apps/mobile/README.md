# Deprecated

This folder previously contained a React Native (Expo) spike.

The project direction has changed to **two separate native apps**:
- `apps/ios`
- `apps/android`

The remaining empty folder can be deleted once your git tooling supports directory removal.
