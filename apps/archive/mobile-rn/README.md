# Archived: React Native (Expo) spike

This folder contains an archived spike from an earlier approach (React Native + Expo).

Current direction is **two separate native apps**:
- `apps/ios`
- `apps/android`
